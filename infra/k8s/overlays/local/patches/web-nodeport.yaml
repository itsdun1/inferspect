# Expose the Next.js web UI on a NodePort so a developer running k3d /
# k3s on their laptop can hit it without configuring an ingress.
apiVersion: v1
kind: Service
metadata:
  name: web
spec:
  type: NodePort
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
