apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-log-consumer
  labels:
    app.kubernetes.io/name: app-log-consumer
    app.kubernetes.io/component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: app-log-consumer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: app-log-consumer
        app.kubernetes.io/component: worker
    spec:
      containers:
        - name: app-log-consumer
          # NOTE: placeholder image — override in prod overlay.
          image: ollive/app-log-consumer:dev
          imagePullPolicy: IfNotPresent
          env:
            - name: VALKEY_URL
              value: redis://valkey:6379/0
            - name: CLICKHOUSE_HOST
              value: clickhouse
            - name: CLICKHOUSE_PORT
              value: "8123"
            - name: CLICKHOUSE_USER
              value: ollive
            - name: CLICKHOUSE_DB
              value: ollive
            - name: CLICKHOUSE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ollive-secrets
                  key: CLICKHOUSE_PASSWORD
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
