# Placeholder — the real ConfigMap "grafana-datasources" is generated by
# the root base/kustomization.yaml configMapGenerator from
# infra/grafana/provisioning/datasources/clickhouse.yaml.
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources-placeholder
  labels:
    app.kubernetes.io/name: grafana
data:
  README: |
    The actual datasources ConfigMap is generated from
    ../../grafana/provisioning/datasources/clickhouse.yaml via
    configMapGenerator. See base/kustomization.yaml.
