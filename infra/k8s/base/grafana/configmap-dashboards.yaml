# Placeholder — the real "grafana-dashboards" and
# "grafana-dashboards-provisioning" ConfigMaps are generated by the root
# base/kustomization.yaml from infra/grafana/dashboards/*.json and
# infra/grafana/provisioning/dashboards/default.yaml respectively.
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-placeholder
  labels:
    app.kubernetes.io/name: grafana
data:
  README: |
    The actual dashboards ConfigMaps are generated via configMapGenerator
    in base/kustomization.yaml. See ../../grafana/.
