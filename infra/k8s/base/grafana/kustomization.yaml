apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Datasource + dashboard ConfigMaps are generated by the root
# base/kustomization.yaml so they can mount the canonical files under
# infra/grafana/ without duplication.
resources:
  - deployment.yaml
  - service.yaml
