apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The clickhouse-init ConfigMap is generated by the root base kustomization
# via configMapGenerator. configmap-init.yaml is documentation only and is
# intentionally NOT listed as a resource.
resources:
  - statefulset.yaml
  - service.yaml
